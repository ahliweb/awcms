{
    "name": "awcms-public-root",
    "version": "2.25.0",
    "private": true,
    "scripts": {
        "dev": "pnpm -C primary dev",
        "build": "cd primary && npm install && npm run build"
    },
    "engines": {
        "node": ">=22.12.0"
    }
}