{
    "name": "@awcms/shared",
    "version": "1.0.0",
    "description": "Shared utilities for AWCMS public portals",
    "type": "module",
    "scripts": {
        "build": "rm -rf dist && tsc -p tsconfig.json",
        "typecheck": "tsc --noEmit",
        "test:storage-guard": "npm run build --silent && node tests/storage-guard.mjs"
    },
    "main": "src/index.ts",
    "types": "src/index.ts",
    "exports": {
        ".": "./src/index.ts",
        "./supabase": "./src/supabase.ts",
        "./sanitize": "./src/sanitize.ts",
        "./types": "./src/types.ts",
        "./tenant": "./src/tenant.ts"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}
